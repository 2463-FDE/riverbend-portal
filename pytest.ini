[pytest]
testpaths = tests
markers =
    integration: tests that require a live Postgres + Redis (and usually the running stack)
    live: tests that call real AWS Bedrock and SPEND MONEY. Skipped unless --live is passed AND a credential is present. Each asserts a hard per-test cost ceiling.
filterwarnings =
    ignore::DeprecationWarning
